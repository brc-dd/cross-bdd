name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    uses: $/.github/workflows/test.reusable.yaml
    with:
      deno_versions: latest
      bun_versions: latest
      node_versions: 22 24 26
